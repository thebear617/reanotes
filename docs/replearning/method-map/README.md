---
title: 方法坐标
icon: table
---

方法坐标表使用统一字段描述典型表征学习方法。主轴可以多选；信号来源区分主要与辅助；机制只描述实现方式。

## 总表

| 方法         | 表征约束               | 主要信号来源       | 辅助来源     | 实现机制                   |
| ------------ | ---------------------- | ------------------ | ------------ | -------------------------- |
| 监督分类     | 对齐—关系              | 人工标注           | —            | 交叉熵、分类头             |
| AE           | 重建—生成              | 数据自指           | —            | 编码器—解码器、重建损失    |
| VAE          | 重建—生成              | 数据自指           | 随机隐变量   | 变分推断、分布正则化       |
| Diffusion    | 重建—生成              | 数据自指           | 人工噪声过程 | 去噪、逐步生成             |
| BERT         | 预测—上下文            | 数据自指           | 语料上下文   | 掩码语言建模               |
| MAE          | 重建—生成＋预测—上下文 | 数据自指           | 原始输入目标 | 掩码图像建模               |
| SimCLR       | 对齐—关系              | 数据自指           | 批内样本关系 | 对比损失、数据增强         |
| MoCo         | 对齐—关系              | 数据自指           | 模型自举     | 对比损失、动量编码器、队列 |
| BYOL         | 对齐—关系              | 数据自指           | 模型自举     | 自蒸馏、动量教师、停止梯度 |
| DINO         | 对齐—关系              | 模型自举           | 数据自指     | 自蒸馏、中心化、锐化       |
| Barlow Twins | 对齐—关系              | 数据自指           | —            | 互相关矩阵、冗余消除       |
| VICReg       | 对齐—关系              | 数据自指           | —            | 不变性、方差、协方差约束   |
| CLIP         | 对齐—关系              | 天然配对           | 数据筛选     | 跨模态对比学习             |
| CPC          | 预测—上下文＋对齐—关系 | 天然上下文         | 数据内负样本 | 自回归预测、InfoNCE        |
| DeepCluster  | 对齐—关系              | 模型自举           | 数据自指     | 聚类、伪标签               |
| 度量学习     | 对齐—关系              | 人工标注或天然配对 | 样本挖掘     | 对比损失、三元组损失       |

## 读表规则

### 目标相同，不代表方法相同

SimCLR、BYOL 和 CLIP 都属于“对齐—关系”，但它们的信号来源和实现机制不同：

- SimCLR 从同一数据的增强视图构造正样本；
- BYOL 额外依赖教师网络产生目标表示；
- CLIP 使用互联网中天然存在的图文配对。

### 机制相同，不代表信号来源相同

对比损失既可以使用人工类别构造样本对，也可以使用数据增强、时间邻近关系或天然图文配对。

### 一个方法可以跨越多个目标

MAE 既要预测被遮挡的上下文，也要重建输入内容；CPC 既预测未来表示，又通过 InfoNCE 组织表示空间。因此它们需要多标签，而不是被迫选择唯一类别。

## 新方法的登记模板

新增方法时依次回答：

1. 它希望表示保留什么信息或形成什么关系？
1. 最不可替代的训练目标来自哪里？
1. 是否还依赖模型、数据变换或其他辅助信号？
1. 它通过什么损失、结构或训练过程实现？

只有当方法需要较长解释时，才在本页增加二级章节；一般情况下增加一行即可。
