---
icon: 🌀
title: 核技巧：一个有趣的中间地带
tags: [补充]
expanded: false
---

核技巧让你用线性方法解决非线性问题：

非线性映射 `φ: x → φ(x)` 把数据映射到高维→在高维空间做线性方法（SVM, PCA）→但不显式计算 φ(x)，而是用核函数 `K(xᵢ, xⱼ) = φ(xᵢ)·φ(xⱼ)`

所以它是**形式上的非线性、算法上的线性**——训练方式还是有闭式解（SVD），但也等价于在原始空间学到了一个非线性流形。
